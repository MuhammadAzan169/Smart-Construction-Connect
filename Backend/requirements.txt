fastapi
uvicorn[standard]
pydantic
python-multipart
bcrypt
httpx
python-dotenv
PyJWT
numpy
pandas
cachetools

# Document extraction (no PyTorch — safe on 512MB free tier)
PyMuPDF
pdfplumber
python-docx
openpyxl

# Postgres document store (Supabase)
psycopg[binary]
psycopg-pool

# ─────────────────────────────────────────────────────────────────────────────
# Heavy local ML — intentionally NOT installed on Render free tier (512MB RAM).
# Semantic search uses an external embeddings API instead (see EMBEDDINGS_* env).
# OCR of scanned images is disabled (PDF/Office/text extraction still works).
# Re-enable ONLY on a plan with >=2GB RAM and set ENABLE_LOCAL_SBERT=1:
#   sentence-transformers
#   faiss-cpu
#   easyocr
# ─────────────────────────────────────────────────────────────────────────────
